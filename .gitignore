# Base de datos local
*.db
*.sqlite

# Variables de entorno
.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
venv/
.venv/
env/

# macOS
.DS_Store

# Uploads temporales
uploads/
*.csv

# Jupyter
.ipynb_checkpoints/

# Frontend (Orbit Copilot)
node_modules/
# NOTA: static/copilot/ (el build de React) SÍ se versiona, porque Railway
# sirve Python puro (sin paso de build de Node). Se regenera con `npm run build`
# en frontend/ y se commitea. Si algún día se agrega build automático en Railway,
# volver a ignorarlo.
mobile/ios/
mobile/assets/
