// Perfil.jsx — perfil, sensor, conexión con el médico (Clinic), tema.
import Placeholder from './_Placeholder.jsx'

export default function Perfil({ theme }) {
  return (
    <Placeholder theme={theme} kind="ritmo" title="Perfil"
      note="Tus datos, tu sensor, la conexión con tu médico y los ajustes de la app." />
  )
}
